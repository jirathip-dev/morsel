# Impl brief — issue-30-set-goals-r3 (FRESH lane, round 3: port reviewed tree + close 5 r2 findings)

Checkout (absolute): `/Users/jirathip/.herdr/worktrees/morsel/issue-30-set-goals-r3`
Branch: `issue-30-set-goals-r3` (cut from main `a249ddd`). Commit + push; NO PR
(orchestrator owns it). Issue #30; LOCKED V3 renders are the design truth.

## Provenance

Branch `issue-30-set-goals` (origin, tip `8f428dc`) carries two verified-good
rounds (provenance mechanics mutation-proven; mock/validation/errors/V3-states
converged per r2 verdict §"Checks that did converge"). But r2 (fresh reviewer)
found 5 remaining gaps — verdict:
~/.hermes/run/review/rev-30-r2-verdict.md. Two-FAIL rule ⇒ you are a FRESH lane:
PORT the reviewed tree (`git diff a249ddd..8f428dc` from the primary checkout —
GoalsEditor.swift, MockRepository.swift, SupabaseMealMutations.swift,
GoalsEditorTests.swift, Models/Types if needed, migrations.test.ts, DELETION of
0008) and fix the 5 findings while porting. Own every line; no blind
cherry-picks.

## The 5 findings (fix ALL; PASS bar)

1. **RPC envelope — the blocker.** `compute_targets(p public.profiles)` takes
   exactly ONE argument `p` (0002_targets.sql:30; server calls
   `rpc('compute_targets', { p: functionInput })`). The ported app code passes
   a flat ComputeTargetsPayload — structurally wrong on the wire. FIX: encode
   the RPC params as `{"p": <functionInput>}` exactly matching
   server/supabase-repository.ts:478-490's envelope (same keys inside p as
   ComputeTargetsFunctionInput: user_id, sex, age_years, height_cm, weight_kg,
   activity_level, diet_goal, goal_weight_kg). Compare your encoded top-level
   keys against server/supabase-types.ts Functions.ComputeTargets.Args.
2. **Response-guard test that BITES.** Add an app repository-level test for
   SupabaseDashboardRepository.saveGoals that captures the RPC/upsert
   body + response (URLProtocol stub or injectable client seam — mock
   view-model suites do NOT exercise Supabase serialization) and PROVES each
   field comparison: mutation-probe it yourself — remove ONE response check
   (e.g. fatG) in a scratch clone → test FAILS; restore → GREEN. Record
   failing test name + exit codes in your report.
3. **Cumulative migration assertions.** db/migrations.test.ts still regex-reads
   0002's text. Move/add assertions to db/postgres-integration.test.ts (which
   APPLIES the chain): after migrations, information_schema must show
   goals.source exists, column_default 'computed', and the check constraint
   allowing exactly computed|manual. (The existing local-postgres harness in
   that file is the vehicle.)
4. **V3 states — implement + test faithfully.** Per the LOCKED renders
   (30-V3-{default,invalid,saved}.png): bordered agent panel (morsel · agent
   voice header), single "writes source: manual/computed" indicator, WHAT
   CHANGES quantitative consequence (kcal left + "See it"), saved banner
   ("Goals saved ✓") state, field-specific invalid copy + red outline. Add
   view-model/render-level tests for EACH named state (not just two).
5. **Validation reconciled with SetGoalsInputSchema:** the schema makes all
   four values OPTIONAL and accepts decimals; the editor requires all four and
   uses .numberPad (no decimal separator). Reconcile: either (a) editor
   validates only non-empty fields and saves partial goals (matching optional
   semantics; UI copy explains), or (b) keep require-all-four as a documented
   UI constraint with a decimal-capable keyboard (.numbersAndPunctuation) and
   a test pinning the divergence as intentional. Choose (b) only if simpler —
   state the choice; tests must pin whichever semantics ship.

## Scope guard

Same file set as the ported tree (+ new test files). NO weight/0007/HealthKit/
workflow/script files; no ci.yml; no server/ changes (server is the reference,
not the target). Revert anything else.

## Gates (run ALL at final head; real numbers)

npm ci + npm test (60), lint/typecheck/build, xcodegen (pbxproj stable),
swiftlint 0 + strict self-audit (no new >120-char lines — the file-wide
line-length disable hid 8 in the last round; don't add more), xcodebuild build
test iPhone 16 sim CODE_SIGNING_ALLOWED=NO (43+ — grows with the new tests).
Report: new head + origin match; per-finding fix map (file:line); the mutation
probe evidence for finding 2; which validation option (a/b) shipped; honest
unverifiables.
