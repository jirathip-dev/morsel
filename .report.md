# `.report.md` — issue #188: bounded revision-aware downsampled thumbnail reuse

- **Branch:** `issue/188-thumbnail-reuse` — pushed; `git ls-remote origin issue/188-thumbnail-reuse` prints the exact tip.
- **Code commit (the head at which the native gate run ran):** `ebf48ccc6456cb783e9be33cc6bee0b0de9b5567` — `fix: add bounded revision-aware downsampled thumbnail reuse (Refs #188)`, the single code commit on `0db1cb5`.
- **Commits after the code commit are docs/evidence only** (this file and `docs/evidence/issue-188-thumbnail-reuse/**`): `0ea2119` records the single full-suite gate run and its successors are report-header fix-ups. Verified with `git diff --stat ebf48cc..HEAD` — it lists only `.report.md` and `docs/evidence/issue-188-thumbnail-reuse/{README.md,counts.txt}`; no production source or test differs between `ebf48cc` and the tip.
- **Lane worktree:** `/Users/jirathip/.herdr/worktrees/morsel/issue-188-thumbnail-reuse`
- **Simulator:** lane `Morsel188-iPhone16`, iOS 26.5, unsigned Debug (`CODE_SIGNING_ALLOWED=NO`) — **not a physical device**. The lane's evidence runs used UDID `40D8CFB4-2345-409C-B971-463CF846BEA0` (reclaimed in lane cleanup); the final gate run below ran on the recreated sim of the same name, UDID `E5EF858B-050B-4866-8EC3-E836ABE5EFC5`
- **Evidence:** `docs/evidence/issue-188-thumbnail-reuse/README.md` + `counts.txt` (raw `ISSUE-188-*` lines)

## Files changed (19 staged; 2507 insertions / 31 deletions)

New modules (fence item 1) under `app/Sources/Morsel/`:

| file | what it is |
| --- | --- |
| `MealThumbnailKey.swift` | the prepared thumbnail's identity (account × object path × revision × target pixel size), the revision value, the request type, the declared display boxes |
| `MealThumbnailPreparer.swift` | off-body preparation (`CGImageSource` decode straight at target size, EXIF-transform honoured, display-exact fit × display scale), FNV-1a content fingerprint, pixel-byte cost |
| `MealThumbnailCache.swift` | the cache: in-flight coalescing, declared LRU capacity + byte budget, counters, account generations, `removeAll(accountID:)`; never retains source bytes |
| `MealPhotoRevisions.swift` | revalidation epochs per (account, object): path-scoped replacement bump, account-wide bump, logout forget, bounded (256, LRU) |
| `LocalFirstRepository+ThumbnailRevision.swift` | the read seam's revision identity (`MealPhotoRevisionProviding`) + the `queuedPhoto` payload accessor moved here for the file-length budget |

**`LocalFirstRepository.swift` changes (fence item 3, disclosed):** the `revisions: MealPhotoRevisions = .shared` init seam; `store`/`revisions` made internal so the across-file extension can reach them (same precedent as `remote`/`snapshotCache` for `LocalFirstMenus`); `attachMealPhoto` bumps the affected object on a queued replacement and the account on a synced attach; `loadMealImage` now reads the shared `queuedPhoto` accessor (identical bytes/semantics).

**`MealPhotoEditorSection.swift` (out-of-fence, disclosed):** the shipped small photo surface now reads through the cache (`MealPhotoSource(repository:)` + `thumbnailCache.thumbnail(...)`, `var thumbnailCache: MealThumbnailCache = .shared`), and its loader is `@MainActor` — the base loader wrote `@State` from whichever executor resumed it, which did not invalidate the view.

**Probe retargets (out-of-fence, disclosed):** `app/photo-edit-contract.test.ts` (pins the section's cache read instead of the old `repository.loadMealImage` call in the body) and `app/photo-pipeline-contract.test.ts` (pins `queuedPhoto` + the queued revision fingerprint in the moved file).

Tests added under `app/Tests/MorselTests/`: `MealThumbnailCacheTests`, `MealThumbnailPreparationTests`, `MealThumbnailReuseEvidenceTests`, `MealThumbnailIntegrationTests`, `MealPhotoRevisionsTests`, `MealPhotoThumbnailSurfaceTests`, `MealThumbnailTestSupport` (fixtures/probes/remote stubs). `app/Morsel.xcodeproj/project.pbxproj` regenerated by `xcodegen`.

## Seam corrections against the brief (re-derived at this base)

The brief's "row-sized consumer" seam is stale in a different way than it says: `JournalFoodRow.swift:85` renders `MealArtworkSlot`, which resolves **bundled illustration PNGs** (#199/#223/#229 — a photo path is deliberately not an input to a row), so it has no account/object/revision semantics to key and needs no wiring (row geometry untouched; neither `JournalFoodRow.swift` nor `FoodArtworkView.swift` was edited). The live small photo consumer at this base is the detail/edit `.photo` figure in `MealPhotoEditorSection` — the brief's own verified live seam — and that is where the cache is wired. There is no full-size/zoom viewer in the app, so "keep the full-size viewer separate" holds by construction: `loadMealImage` still returns the object's own bytes (asserted in `MealThumbnailIntegrationTests`).

## Per-gate RAW exits (this lane, raw `PIPESTATUS[0]`)

```
npm ci --no-audit --no-fund                 → added 256 packages in 7s
npm run typecheck                           → typecheck=0
npm run lint                                → lint=0
npm test                                    → npmtest=1   (see classification below; second run after the probe fixes)
npx vitest run app/photo-edit-contract.test.ts app/photo-pipeline-contract.test.ts → probes=0 (17/17)
npx vitest run --testTimeout=60000 server/http.test.ts server/render-png.test.ts \
    server/tool-classification.test.ts      → diagnostic=0 (12/12; DIAGNOSTIC, not gate evidence)
cd app && xcodegen generate ; git status --short -- project.pbxproj
                                            → " M app/Morsel.xcodeproj/project.pbxproj"; re-run byte-stable
                                              (sha256 59442cb6…f457 both runs) = xcodegen byte-stable: yes
cd app && swiftlint --strict                → swiftlint=0 (125 files, 0 violations)
git diff --check                            → diffcheck=0 ; git status --short → 19 staged, .brief.md/.report.md/.lane-logs untracked
```

**`npm test` = 1 classification (honest):** second run: 5 failed / 551 passed / 556, **zero `AssertionError`**, all five in `server/**` (`http`, `render-png` ×2, `tool-classification` ×2) with "Test timed out in 5000ms"; host load 18–29 during the run (10 CPUs). The first run additionally failed the two probes this lane retargets (2 real `AssertionError`s, since fixed — see above; the probes now pass). The diagnostic rerun of exactly the timed-out files with `--testTimeout=60000` passes 12/12 (each test 2.8–9 s). No timeout, no test and no repo config was weakened; the suite is load-sensitive on this shared host (documented class).

**Native suite — ONE complete full-suite invocation (the gate):** the brief's
exact command with no skip/only flags at the delivered head,
`HERDR_XCODEBUILD_DIRECT=1 xcodebuild test -project Morsel.xcodeproj -scheme Morsel -destination "platform=iOS Simulator,id=E5EF858B-050B-4866-8EC3-E836ABE5EFC5" CODE_SIGNING_ALLOWED=NO -derivedDataPath /tmp/xc-dd-188`:

```
Executed 373 tests, with 0 failures (0 unexpected) in 33.986 (34.428) seconds   (63 suites)
** TEST SUCCEEDED **
xcodebuild=0
```

The documented fresh-process `GoalsPolishTests` wedge did hit the FIRST full
invocation (frozen at its first case, xcodebuild 0.0% CPU, 167 cases in; killed
by the recovery procedure → raw `xcodebuild=143`, `.lane-logs/xcodebuild-fullrun1.log`),
and the single warm rerun on the SAME simulator completed the whole suite —
`.lane-logs/xcodebuild-fullrun2.log` is that complete run. The earlier split
legs (`-skip-testing` / `-only-testing`, 368/0 + 5/0) are superseded by this
single-run truth and are retained only as recovery context.

**Native test count: 373 tests, 0 failures, in ONE complete invocation**, on
UDID `E5EF858B-050B-4866-8EC3-E836ABE5EFC5`.

## RED / GREEN exits

```
RED  (scratch /tmp checkout of base 0db1cb5 + base-compatible probe
      /tmp/morsel-188-red, drives loadMealImage bytes + UIImage(data:) in the body):
      ISSUE-188-RED-PROBE appearances=2 base_fetches=2 base_pixels=2400x1200 display_target_pixels=1290x435
      ISSUE-188-RED-PROBE concurrent_appearances=6 base_fetches=6
      XCTAssertEqual failed: ("2") is not equal to ("1") - two appearances must share one fetch
      XCTAssertTrue failed - a warm revisit must reuse the prepared image
      XCTAssertLessThanOrEqual failed: ("2400.0") is greater than ("1290.0") - the body must consume
        prepared pixels, not the full-size original
      XCTAssertEqual failed: ("6") is not equal to ("1") - identical concurrent reads must share one fetch
      redprobe=65                      ← behavioural RED (assertion failures, not a compile error)

GREEN (head ebf48cc, same invariants through the prepared-thumbnail cache):
      xcodebuild=0   # ONE complete full-suite invocation, no skip/only flags:
                     # "Executed 373 tests, with 0 failures" / ** TEST SUCCEEDED **
                     # (first attempt wedged in GoalsPolishTests → killed, xcodebuild=143;
                     #  warm rerun on the SAME sim completed — see the gate section)
```

## Counts / decode counts / memory evidence (raw, from the final green native run)

```
ISSUE-188-COUNTS phase=cold fetches=1 preparations=1 hits=0 resident=1 resident_pixel_bytes=1513800
ISSUE-188-COUNTS phase=warm fetches=1 preparations=1 hits=1 resident=1 resident_pixel_bytes=1513800
ISSUE-188-COUNTS phase=concurrent callers=6 fetches=1 preparations=1 coalesced=5 hits=0
ISSUE-188-COUNTS phase=scale scale3=870x435 scale2=580x290 fetches=2 preparations=2
ISSUE-188-COUNTS phase=replacement fetches=2 preparations=2 resident=1 served=blue
ISSUE-188-COUNTS phase=queued_to_remote fetches=2 preparations=1 reuses=1
ISSUE-188-COUNTS phase=external_change warm=1 signalled=2
ISSUE-188-COUNTS phase=accounts fetches=3 resident=2
ISSUE-188-MEMORY capacity=2 entries=2 pixel_bytes=3027600 evictions=5 byte_budget_entries=2 byte_budget_bytes=6400 budget=8000
ISSUE-188-SURFACE cold_fetches=1 cold_preparations=1 warm_fetches=1 warm_preparations=1 warm_hits=1 remote_loads=1 target_pixels=1290x435
ISSUE-188-TRACE seam=queued_to_remote fetches=2 preparations=1 remote_loads=1
ISSUE-188-TRACE seam=queued_replace fetches=2 preparations=2 served=blue
```

Reading them: **cold** = 1 fetch / 1 preparation / 1 513 800 resident pixel bytes (870×435×4 = the display-exact pixels; no original bytes retained). **Warm** = +0 fetches, +0 decodes, 1 hit. **Six identical concurrent reads** = 1 fetch / 1 preparation / 5 coalesced joins. **3x vs 2x** = two identities (870×435 vs 580×290 prepared pixels for a 430×145pt box) and two preparations — the prepared pixels ARE the painted pixels. **Same-path replacement** = 1 refetch / 1 decode, the replaced entry dropped (1 resident) and the replacement served (colour probe). **Queued→remote** (identical payload, new revision) = 1 refetch / 0 extra decodes (prepared pixels re-filed). **External change with no app signal** = 0 extra fetches (never polled); the app's signal = exactly 1 refetch. **Accounts**: same path under two accounts = two identities, no pixel/byte re-use across accounts; logout drops one account (entries 2→1) and the other stays warm; an in-flight attempt retired by logout publishes nothing. **Memory**: capacity 2 → 2 residents / 5 evictions (LRU — the oldest identity is the one re-fetched); an 8 000-byte budget with 3 200-byte thumbs settles at 6 400 bytes; one over-budget entry is kept and served, never thrashed; the preparation worker's peak concurrency is 1. Corruption/missing bytes are refused with nothing cached (`residentEntryCount()==0`, 0 decodes) and do not poison the identity; a cancelled caller stops waiting without cancelling the shared attempt (the survivor still gets the image and the cache stays warm).

## Orientation evidence

```
ISSUE-188-ORIENTATION source=3200x2400 output=200x150 off_main=true red_on_top=true red_on_bottom=false
ISSUE-188-SURFACE mounted_body painted=red image=870x435 fetches=1 preparations=1
```

A synthetic JPEG with RAW pixels 2400×3200 carrying EXIF orientation 6 is read as 3200×2400 (the quarter turn is applied and read from the metadata without decoding) and prepared at exactly 200×150 for a 430×150pt box at 1x; the RAW left (red) half lands on the display TOP (a control proves the probe measures top-left-origin regions, and an orientation-ignoring path would paint the RAW halves as columns). The preparation body ran off the calling (main) actor. A mounted SwiftUI body awaiting the same cache seam paints the photo (red) at the display-exact 870×435 prepared size.

## What I could NOT verify (disclosed, not papered over)

1. **The shipped figure's own painted pixels** could not be read back through `drawHierarchy` in the unit bundle: the section's `.task` loader runs deferred under the test pump and the capture keeps the previous frame. The shipped surface is therefore proven by its **counted** read (1 fetch + 1 preparation cold, +0 warm, through the injected repository, target pixels 1290×435) plus the contract probe pinning that the body no longer calls `loadMealImage`/constructs the image from fetched bytes; the paint path is proven by the mounted-body probe above.
2. **External (other-device) same-path photo replacements are not polled.** The declared, tested bound is: zero extra fetches while warm; revalidation happens on an app-side signal (path-scoped replacement, account-scoped attach, logout/account switch). A stale window therefore exists until the next signal — stated as the rule, not hidden.
3. **`MealThumbnailCache.removeAll(accountID:)` has no production call site** in this lane (the session teardown that would call it is outside the fence); it is implemented and tested at the cache level (account isolation, in-flight retirement).
4. **No physical-device behaviour, no visual/design review, no server/DB gate** (no server/DB contract changed; the 5 `server/**` npm-test failures are the host load-skew class, diagnosed above).
5. `npm test` raw exit is 1 on this host (load-skew timeouts in `server/**`); the same files pass with a diagnostic 60 s timeout. Reported as-is.

NOT MERGED; not opened as PR; no deploy; no production writes.
