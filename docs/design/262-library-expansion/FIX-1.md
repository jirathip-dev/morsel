# Issue 262 · fix 1 · Pad thai metadata correction

Authority: owner-routed `.brief-fix-1.md`, product base `d499c1a25b36b588ee6689e64d1f6ad233f7cffb`. Surface: Compare. This is a bounded text/accessibility correction, not a new art direction or owner pixel approval.

## F1 — text now matches the existing artwork

Current description and gallery alt text:

> Broad folded flat noodles on a shallow plate with sprouts and a lime wedge; no particular protein or nut garnish is specified.

Only the Pad thai `description` changed in:

- `library/subjects.json`
- `library/catalog.json`
- `batch-5/subjects.json`
- `batch-5/catalog-delta.json`

Both `batch-5/gallery-paper.html` and `batch-5/gallery-night.html` were regenerated from the corrected generator. They expose the actual DOM alt value to the browser gate. That gate requires the corrected value at 390×844 and 1440×1000 in both themes. All other subjects, aliases, categories, metadata envelopes and Pad thai non-description fields remain unchanged, verified against the frozen before receipt.

The historical proposal files in `references/` remain byte-identical. Their superseded Pad thai description is not current artifact metadata. The corrective override in `scripts/pipeline.py` makes full future regeneration retain the fix. Rejected wording also appears deliberately in the retained RED regression log; it is not a current description.

## F2 — exact source and art preservation

`library/sources/pad-thai.svg`

- Before SHA-256: `25fb62084e54b63f7132300c781d948474ccd4ef92f7ff2166829ee896de0c14`
- After SHA-256: `25fb62084e54b63f7132300c781d948474ccd4ef92f7ff2166829ee896de0c14`
- Equality: **PASS**.

All **1,170** source/master/export files remain byte-identical. No source SVG, compiled master or art PNG was rewritten by the fix runner. All **117** existing proof PNGs, including regenerated batch-5 contacts, optical proofs and browser captures, remain SHA-256-identical. The root index was regenerated through its canonical generator and also remains byte-identical. Existing visual review therefore still describes these same pixels; no new approval is inferred.

Machine proof: `evidence/fix-1/before.json` and `evidence/fix-1/verification.json`.

## F3 — refreshed dependency chain

`scripts/fix_pad_thai_metadata.py` changes only the four descriptions, regenerates batch-5 proof pages/contact sheets and the canonical aggregate index, reruns the full batch-5 browser matrix, then checks the source/proof hashes and cumulative evidence. No artwork export command is needed or invoked.

The affected gallery HTML input hashes are renewed in `batch-5/browser.json`. Fresh named browser logs bind real DOM alt values to both themes and widths. The batch-5 and root SHA-256 manifests are regenerated after the reports and evidence settle. The delivered receipt lists their changed entries and final counts.

The aggregate index contains no per-subject description; its generated bytes and existing input/output pins remain valid. Earlier batch proofs and all shipped assets remain untouched.

## F4 — raw gate results

| Gate | Raw exit | Result / named evidence |
|---|---:|---|
| Pre-fix metadata regression | 1, expected | `evidence/fix-1/metadata-red.log`: six tests, seven assertion failures including metadata subcases; the unchanged-art and unchanged-pixel controls passed; no harness error |
| Proof regeneration | 0 | `evidence/fix-1/proofs.log` |
| Canonical aggregate index regeneration | 0 | `evidence/fix-1/aggregate-index.log`; bytes unchanged |
| Cumulative closed-set gate | 0 | `evidence/fix-1/closed-set.log` |
| Real Chromium browser gate | 0 | `evidence/fix-1/browser.log`; 10 captures, no console errors or overflow; actual corrected alt checked in four gallery cases |
| Corrected metadata regression | 0 | `evidence/fix-1/metadata-green.log`; 6 tests passed |
| Retained art contracts | 0 | `evidence/fix-1/art-contract.log`; 13 tests passed |
| Corrected evidence contracts | 0 | `evidence/fix-1/evidence-contract.log`; 10 tests passed |
| Historical verifier negative control | 1, expected | `evidence/fix-1/historical-verifier-red.log`; original eight expected failures, two valid controls, no harness error |
| Cumulative preservation and evidence | 0 | `evidence/fix-1/preservation-and-evidence.log` |

Exact argument lists, raw exits and named log paths: `evidence/fix-1/commands.json`. The historical verifier control adapts only its old subjects description in memory; this is isolated test-fixture compatibility, not a relaxation of the corrected gate or a write to current metadata.

The first publication attempt stopped before committing because unittest emitted trailing whitespace in the new metadata RED log. Only that trailing whitespace was trimmed in the committed log; every result and the raw exit remain unchanged. Its exact original bytes are retained lane-locally as `.lane-logs/fix-1-metadata-red-raw.log`. No test was rerun to alter a result.

Counts are unchanged: **107 new food identities / 120 total foods / 130 assets**. Shipped preservation is unchanged: **18 identities / 162 art files / 534 protected product files**. All 12 batch-5 identities remain evidence-free for this account, 0 observed rows, general coverage only.

The independent repository `npm test` result remains raw exit 1: 583 passed / 2 failed, plus two worker timeout errors. It was not rerun or waived in this bounded round. No native gate was launched.

## Reproduction and delivery boundary

From this bundle:

```sh
PYTHONDONTWRITEBYTECODE=1 python3 scripts/fix_pad_thai_metadata.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/fix_pad_thai_metadata.py --check-only
PYTHONDONTWRITEBYTECODE=1 python3 scripts/pipeline.py pack --batch 5 --private-names /path/to/private/logged-names.json
PYTHONDONTWRITEBYTECODE=1 python3 scripts/pipeline.py check-package
```

Browser work obeyed the existing coordinator predicate, rechecked in bounded groups. No shared lock was invented; no sibling was signalled. Browser profiles were created only in the owned scratch path and removed. No duplicate artwork tree or native build output was created.

The correction is delivered as a new artifacts-only commit and a hash-identical product documentation mirror. Final pixel approval remains outstanding. No app, Swift, DB, schema, matcher, production asset catalog or bundling changes.

DESIGN ARTIFACTS ONLY; no app/DB edits; not merged; not opened as PR; no deploy.
