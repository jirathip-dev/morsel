let theme='paper';function update(){const group=document.querySelector('#group').value;document.documentElement.dataset.theme=theme;for(const b of document.querySelectorAll('[data-theme-choice]'))b.setAttribute('aria-pressed',String(b.dataset.themeChoice===theme));for(const el of document.querySelectorAll('[data-group]'))el.hidden=group!=='all'&&el.dataset.group!==group;for(const a of document.querySelectorAll('[data-page]')){a.href=`${a.dataset.page}.html?theme=${theme}&version=${a.dataset.version}`;const img=a.querySelector('img');img.src=`evidence/${a.dataset.page}-${theme}-${a.dataset.version}.png`;img.alt=`${a.closest('article').querySelector('h3').textContent} · ${theme} · ${a.dataset.version}`;}for(const a of document.querySelectorAll('[data-contact]')){a.href=`evidence/contact-${a.dataset.contact}-${theme}.png`;a.textContent=`Contact sheet · ${theme==='paper'?'Paper':'Night'}`;}window.galleryState={theme,group};}for(const b of document.querySelectorAll('[data-theme-choice]'))b.addEventListener('click',()=>{theme=b.dataset.themeChoice;update()});document.querySelector('#group').addEventListener('change',update);update();window.ready=true;
