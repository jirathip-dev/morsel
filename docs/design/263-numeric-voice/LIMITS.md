# State coverage and limits

## Blocking native finding

SwiftUI tabular alignment: **UNVERIFIED**, not PASS and not FAIL.

The brief simultaneously requires actual SwiftUI alignment proof and fences “no Swift”. A focused request for permission to use an isolated evidence-only SwiftUI probe received no response. The strict fence was preserved: no Swift file authored, no inline Swift code run, no Xcode build, no simulator app modified. Existing four `.monospacedDigit()` calls use IBM Plex Mono at this base and cannot witness the proposed EB Garamond result.

The gallery and its alignment pages are genuine Chromium renders of local HTML. Raw DOM Range advances, platform-font records, natural digit columns and a proportional negative control prove tabular shaping in that renderer only. They must not be promoted into a native success claim. The full native acceptance criterion remains open; owner implementation approval is still required.

## Scope normalization and source discrepancies

- The issue says “six groups” but lists five affected bullets plus technical. The gallery splits weight/chart labels from rows/columns to make six affected groups, with kept mono separately. Nothing is dropped.
- Broad source scan has 22 matching files, not the issue's older 20-file inventory. All are individually accounted for in coverage.json and the independent audit.
- A 30-point Goals gauge cannot be re-minted from this base: no Goals consumer of `morselGauge` exists. Current Goals uses 22/17-point ruled fields; those are shown. Only History calls the 30-point SummaryMetric.
- Primary WeightTrendView axes are hidden. Visible 9-point dates belong to WeightDeltaBand. Its caption goes through SectionHeading; current formatter rounds those caption numbers to whole values. No invented hidden axis is shown.
- `morselTag` is unused at this base. It has no visible instance to render.
- Settings replay/delete chevrons are SF Symbols carrying a font modifier, not mono strings. Their native font-metric effect remains unverified and they are explicitly excluded from text specimen counts.

## Rendered specimen states

See generated COVERAGE.md and coverage.json for every page and exact source ID. Headline normal/zero/large/missing; food portions, macros, absent macros and missing calories; single/multiple log timestamps; full/absent macro targets; ledger/over/under/on-target comparisons; all eight gutter hosts; calendar range/day/selected/future; weight captions, symbols, populated and unavailable receipt values; filled/invalid/blank Goals fields; current training receipt confirmed/unconfirmed/unavailable and blank amount placeholder; capture/edit/menu numeric fields; confidence high/low/missing; Add/Edit photo notices, metadata and illustration provenance; endpoint configured/unconfigured, both setup prompts, Copy/Copied and wordmarks.

These are finite typography specimens, not exhaustive runtime/data permutations. Reused helpers are expanded in the source audit and mapped to the shared rendered excerpt; that is not a claim to have re-minted each host in the native app.

## Not re-minted / not certified

- No current-device, signed-in, production-account or native before/after screenshots. All comparison shots are visibly labelled source-derived HTML excerpts.
- Full native screen geometry, SF Symbols, text-field focus/keyboard, Dynamic Type, localization, VoiceOver and custom-font fallback are not certified.
- Swift Charts graph geometry, tap-selection and marker glyph behavior are not certified. Chart marks are fixed illustrations supporting type context, not measured weight/nutrition.
- Actual photo bytes, camera/picker permission screens, expired URL recovery and native photo surfaces are not fetched or rendered. Only their relevant text is shown; no fake meal photograph.
- No HealthKit read, missing/denied/stale permission flow, authentication, clipboard write, persistence or API request occurs. The half-(a) interactive training sheet is a separate lane, not duplicated here.
- This font-only study shows the old training receipt to cover its current font consumers, not to restore that receipt on Today. Half-(a) owner decisions remain locked.
- Specimen controls do not save or navigate native screens. Gallery theme/filter/link controls work; pointer-driven theme change is tested, group filtering uses an explicitly named change-event probe.

## Classification notes, not reopened decisions

Nontechnical inherited mono prose moves to serif; numeric units move with their values. JPEG/KB moves as user-facing metadata. Endpoint status stays with the explicitly retained Settings technical block. Wordmarks stay as the issue's named brand exception. The independent audit's stricter source-only classifications are preserved unchanged; this design disposition is stated here rather than rewriting the audit.

## Next gate

Owner can review the browser type direction now. Before claiming the native proof requirement met, obtain permission for an isolated native evidence probe and measure actual EB Garamond `.monospacedDigit()` rendering at these sizes/weights, with native negative control and screenshots. A feature-table or CoreText-only check is not a substitute. Do not open an implementation lane from this bundle alone.

DESIGN ARTIFACTS ONLY; no app/DB edits; not merged; not opened as PR; no deploy.
