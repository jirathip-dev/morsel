const query=new URLSearchParams(location.search);const theme=query.get('theme')==='night'?'night':'paper';const version=query.get('version')==='after'?'after':'before';document.documentElement.dataset.theme=theme;document.documentElement.dataset.version=version;document.querySelector('#edition').textContent=`${theme==='night'?'Night':'Paper'} · ${version==='after'?'AFTER':'BEFORE'}`;for(const image of document.images)image.src=image.getAttribute('data-src').replace('__THEME__',theme);window.ready=true;
