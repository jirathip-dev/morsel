# Fix contract — impl-30-r3 compile gate correction

Branch: issue-30-set-goals-r3, current head `0c0231885c5052447d4f93d93a7c8bda6034669e`.
This is a real orchestrator gate failure found by direct xcodebuild against booted
simulator UDID 59DDC0C5-891E-4EC0-91AF-4F50DF68D793.

`app/Sources/Morsel/MorselApp.swift:188` calls:
`GoalsEditorView(repository: repository, userID: userID, onSeeToday: showToday) { ... }`
but the initializer expects labeled arguments `repository:userID:onSaved:onSeeToday:`.
The trailing closure is being interpreted as an unlabeled argument and compilation
fails: incorrect argument label (have repository:userID:onSeeToday:_, expected
repository:userID:onSaved:onSeeToday:).

Minimal fix ONLY: label the closure `onSaved: { await dashboardViewModel.load() }`
and pass `onSeeToday: showToday` as its existing labeled argument (or equivalent
explicit closure syntax matching the initializer). Do not change behavior or scope.

Then run directly on the booted iPhone 16 UDID (no wrapper substitution):
`xcodebuild build test -project app/Morsel.xcodeproj -scheme Morsel -destination 'id=59DDC0C5-891E-4EC0-91AF-4F50DF68D793' CODE_SIGNING_ALLOWED=NO`
plus swiftlint --strict, xcodegen stability, and npm gates if needed. Commit + push
and report the new head. No PR operations; orchestrator will re-review the corrected
head. Do not touch #31/#34 or unrelated files.
