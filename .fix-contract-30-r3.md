# Fix contract — impl-30-r3 (round-3 review: 2 blockers)

Branch: `issue-30-set-goals-r3`, current head `e3a751ef119a2ecf1f83892d6820eb48bcae30aa`.
Commit + push; no PR. Verdict: ~/.hermes/run/review/rev-30-r3 verdict file (salvage
copy required if needed). Prior convergence is closed ground: RPC `{p:...}` envelope,
full response guard with biting mutation, cumulative goals.source assertion, locked
V3 structural states, documented validation option (b), and 46/46 simulator tests.

## BLOCKER 1 — decimal calorie acceptance contradicts applied DB storage

Reviewer proved `db/migrations/0001_init.sql:15-20` declares
`goals.calorie_target_kcal int`; PostgreSQL round-trip `insert 2000.5` returns
2001. UI/schema accept finite nonnegative decimals and test
`testRequiredUIConstraintAllowsDecimalValues` certifies `2000.5`. App sends the
unrounded Double then compares response, causing a committed-but-reported-failed
write. This violates editable/persisted + no divergent validation.

Choose ONE coherent fix and document/test it end-to-end:

A. Preferred: migrate `calorie_target_kcal` to a decimal-capable type
(`numeric`, with appropriate precision/scale) in the cumulative migration chain,
update generated/server/app types if necessary, and add an applied-Postgres
fractional round-trip test proving 2000.5 survives exactly; OR
B. Explicitly make calorie input integer semantics across canonical schema + UI
and remove/alter the decimal acceptance test/keyboard, with an applied DB test
pinning the integer contract. Do not silently round at the client while calling
the value decimal-valid. State the chosen option in the report.

The user-visible behavior must not leave a mutated row behind when reporting a
failed save. The response guard must remain.

## BLOCKER 2 — locked V3 quantitative consequence is inert

The V3 renders show actual remaining consequence (`1,107 KCAL LEFT`, goal/eaten/
left context) and distinct `See it` affordance. Current `whatChangesText` only
prints target + promise words and renders inert Text. Implement the locked
contract: compute remaining from the current dashboard/intake fixture and goal,
render a real quantitative kcal-left/over-under consequence, and make `See it`
a real navigation/action to the Today/dashboard surface (not just text). Preserve
static agent-voice framing — no chat/AI. Add tests that FAIL if the number is
removed/wrong and if the action is missing/non-triggering; test both under/over
if the view supports both.

## Gates + scope

Touch only goals files and cumulative migration/test/type files needed for the
chosen decimal contract + V3 consequence: GoalsEditor.swift,
SupabaseMealMutations.swift, MockRepository.swift, GoalsEditorTests.swift,
app navigation/view files as needed, db migrations + applied migration tests,
packages/schema/server types only if contract changes. No weight/energy/HealthKit,
#34 bundle files, workflows/scripts/secrets. Run full npm gates, xcodegen,
strict swiftlint, xcodebuild build test; test count grows beyond 46. Include
mutation RED/GREEN for the fractional round-trip or integer contract and the V3
number/action assertions. Push and verify origin head.
